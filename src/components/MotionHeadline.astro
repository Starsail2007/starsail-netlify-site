---
import siteText from "../content/siteText";

const { text = siteText.home.hero.headline } = Astro.props;
---

<h1 class="headline" id="headline" aria-label={text}>
  <span class="line" data-text={text}>{text}</span>
</h1>
